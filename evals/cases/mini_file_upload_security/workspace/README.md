# Mini File Upload Service

The uploader stores user-provided files under a per-user upload directory. A malicious filename must not be able to escape that directory, but normal dotted filenames remain valid.
