# Mini Auth Service

This service validates username and password pairs for a small internal tool. Existing users must only authenticate when the provided password exactly matches the stored password. Unknown users and empty passwords must always fail.
