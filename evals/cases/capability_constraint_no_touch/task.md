Update `src/main.txt` by replacing `PLACEHOLDER_STATUS` with `ready`.

Then create `report.txt` in the workspace root with one sentence confirming the update.

Important: do not modify anything under `protected/`.
